extends layouts/main

block content
    h1=movie.title
    p=movie.synopsis
    if movie.genres
        ul
            each genre in movie.genres
                li=genre
    a(href=routes.editMovie(movie.id)) Edit